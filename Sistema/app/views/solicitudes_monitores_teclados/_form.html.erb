<%= form_for(@solicitudes_monitores_teclado) do |f| %>
  <% if @solicitudes_monitores_teclado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solicitudes_monitores_teclado.errors.count, "error") %> prohibited this solicitudes_monitores_teclado from being saved:</h2>

      <ul>
      <% @solicitudes_monitores_teclado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :numero_de_formulario %><br>
    <%= f.number_field :numero_de_formulario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
