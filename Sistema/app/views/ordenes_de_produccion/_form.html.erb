<%= form_for(@orden_de_produccion) do |f| %>
  <% if @orden_de_produccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orden_de_produccion.errors.count, "error") %> prohibited this orden_de_produccion from being saved:</h2>

      <ul>
      <% @orden_de_produccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :nro_formulario %><br>
    <%= f.number_field :nro_formulario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
