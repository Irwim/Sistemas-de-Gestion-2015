<%= form_for(@categorias_componente) do |f| %>
  <% if @categorias_componente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorias_componente.errors.count, "error") %> prohibited this categorias_componente from being saved:</h2>

      <ul>
      <% @categorias_componente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
