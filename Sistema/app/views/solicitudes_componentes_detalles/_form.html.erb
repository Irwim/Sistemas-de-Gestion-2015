<%= form_for(@solicitudes_componentes_detalle) do |f| %>
  <% if @solicitudes_componentes_detalle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solicitudes_componentes_detalle.errors.count, "error") %> prohibited this solicitudes_componentes_detalle from being saved:</h2>

      <ul>
      <% @solicitudes_componentes_detalle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidad_solicitada %><br>
    <%= f.number_field :cantidad_solicitada %>
  </div>
  <div class="field">
    <%= f.label :cantidad_entregada %><br>
    <%= f.number_field :cantidad_entregada %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
