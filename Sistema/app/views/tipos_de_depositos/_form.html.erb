<%= form_for(@tipos_de_deposito) do |f| %>
  <% if @tipos_de_deposito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipos_de_deposito.errors.count, "error") %> prohibited this tipos_de_deposito from being saved:</h2>

      <ul>
      <% @tipos_de_deposito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
